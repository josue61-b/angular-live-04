export interface Product {
  id: number;
  name: string;
  category: string; // Added category property
  price: number;
}

export interface ProductForm {
  name: string;
  category: string; // Added category property
  price: number;
}
